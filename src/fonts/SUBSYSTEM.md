# fonts

**Wishlist #:** 12
**Status:** stub — owned by Wave 5 agent.

WOFF2 upload (reuses asset pipeline), signature validation, `@font-face` emission, Style Kit token integration via `font:<contentHash>` references.
