# themes

Owner-authored Style Kit (`customStyleKit`) with contrast guard reusing `src/theme/contrast.ts`. Sibling `visitor-mode/` directory hosts light/dark dual-palette resolution + toggle element.

> Distinct from `src/theme/` (token derivation helpers; pre-existing).
