# canvas/responsive

**Wishlist #:** 1
**Status:** stub — owned by Wave 1 agent.

Translates positioned-canvas px into fluid published layout across three named breakpoints (desktop / tablet / phone). Honors optional per-Element `responsive` overrides.
