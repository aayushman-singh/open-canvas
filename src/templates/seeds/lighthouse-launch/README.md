# lighthouse-launch

Product launch page for a fictional release-engineering platform. Hero with
headline, sub and two CTAs. Three-column feature grid of pain points removed.
Three-column pricing section built from `columns` + `paragraph` nodes (Starter
/ Team / Enterprise). Footer with anchor links.
