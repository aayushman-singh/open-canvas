# forms

**Wishlist #:** 7
**Status:** stub — owned by Wave 2 agent.

Form ElementType, submit handler (Turnstile + rate-limit), `formSubmission` storage, optional webhook delivery, Owner inbox + CSV export. `FormRateLimiter` DO.
