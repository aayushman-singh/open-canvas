// src/editor-client/types.d.ts
//
// Ambient declarations for the editor-client subtree. Scoped here
// rather than in src/types/ so the Worker-side typecheck doesn't see
// declarations only the editor-client needs.

declare module '*.css';
